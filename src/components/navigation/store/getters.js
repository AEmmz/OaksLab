export default {
	mobileMenu(state) {
		return state.mobileMenu;
	},

	collectionFilter(state) {
		return state.collectionFilter;
	},

	menuOverlay(state) {
		return state.menuOverlay;
	},
	statisticsFilter(state) {
		return state.statisticsFilter;
	},
};
